<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kbg.game.conifg.mapper.game.GameMapper">

    <select id="selectCharacterClassCritical">
        SELECT
            *
        FROM character_class_critical
        WHERE character_class = #{characterClass}
    </select>

    <insert id="insertCharacterClassCritical">
        INSERT INTO `character_class_critical`(
            `character_class`,
            `class_node`,
            `skill`,
            `back_attack_yn`,
            `first_ring`,
            `second_ring`,
            `elixir_yn`,
            `adrenaline`,
            `ark_passive_node`
        ) VALUES(
            #{characterClass},
            #{classNode},
            #{skill},
            #{backAttackYn},
            #{firstRing},
            #{secondRing},
            #{elixirYn},
            #{adrenaline},
            #{arkPassiveNode}
        ) ON DUPLICATE KEY UPDATE
            `class_node` = #{classNode},
            `skill` = #{skill},
            `back_attack_yn` = #{backAttackYn},
            `first_ring` = #{firstRing},
            `second_ring` = #{secondRing},
            `elixir_yn` = #{elixirYn},
            `adrenaline` = #{adrenaline},
            `ark_passive_node` = #{arkPassiveNode}
    </insert>


</mapper>